
public interface CanSwim {
    double swim(Pool pool); // тут маленько запутался, что именно тут происходит?
}                           // класс Pool со значением pool?
                            // или Pool подтягивает значения из нашего класса Pool?
                            // и что такое double почему именно double